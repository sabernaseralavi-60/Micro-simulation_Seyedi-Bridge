---
title: "ساماندهی ترافیکی تقاطع زیرپل سیدی، کرمان"
subtitle: "گزارش فاز ۱ — اسکلت متحرک (Walking Skeleton)"
author: "تیم مطالعه (Claude Code، به‌سرپرستی صابر ناصرعلوی)"
date: today
---

::: {.callout-important}
## هشدار — مدل کالیبره‌نشده
این گزارش نسخهٔ **فاز ۱ (اسکلت متحرک)** است. هندسهٔ شبکه از OSM خام و تقاضا
کاملاً **فرضی** (حدس مهندسی، نه برداشت میدانی) است. نتایج این نسخه **صرفاً برای
آزمودن خط لولهٔ تکرارپذیر** معتبرند، نه برای هیچ نوع مقایسهٔ سناریویی یا
پیش‌بینی مقدار مطلق. مقایسهٔ سناریوها و تحلیل حساسیت در فازهای ۲ تا ۴ (پس از
واردکردن هندسهٔ دقیق و تقاضای مبتنی بر شواهد) انجام می‌شود.
:::

## ۱. هدف این نسخه

هدف فاز ۱، طبق نقشهٔ راه پروژه (`CLAUDE.md`)، ساخت یک خط لولهٔ **کامل و
end-to-end** است — از دادهٔ خام OSM تا گزارش منتشرشده — حتی با هندسه و تقاضای
خام. این گزارش صحت فنی خط لوله را نشان می‌دهد، نه یافته‌های مهندسی نهایی.

## ۲. ساخت شبکه و راستی‌آزمایی جدایی تراز

شبکه از OpenStreetMap (Overpass API، bbox حول تقاطع زیرپل سیدی) با `netconvert`
ساخته شد. جدایی تراز عرشهٔ پل (بزرگراه امام‌رضا، `bridge=yes layer=1`) از شبکهٔ
هم‌سطح زیرین (جادهٔ سیدی/بلوار شهید سلیمانی) به‌صورت **بصری بررسی و تأیید** شد
(شکل @fig-layer-check) — نقطه‌ای که طبق `CLAUDE.md` مهم‌ترین ریسک خطای مدل است.

![بازرسی چشمی جدایی تراز: قرمز = عرشهٔ پل (z>0)، آبی = شبکهٔ هم‌سطح (z=0). عرشه بدون اتصال از روی شبکهٔ زیرین عبور می‌کند.](../outputs/figures/network_layer_check_zoom.png){#fig-layer-check width=85%}

## ۳. تقاضا: روش jtrrouter

طبق توجیه روش‌شناسی `CLAUDE.md`، برای یک تقاطع منفرد، حجم ورودی هر پا + نسبت
گردش‌ها روشی به‌مراتب کم‌نیازتر از ماتریس OD کامل است. تقاضای این نسخه کاملاً
فرضی است (رجوع به `config/assumptions.yml` بخش‌های `demand_baseline_phase1` و
`traffic_control.turn_ratio_baseline`):

| پا | حجم ورودی فرضی (وسیله/ساعت) |
|---|---|
| غرب (عرشهٔ پل) | ۸۰۰ |
| شرق (عرشهٔ پل) | ۸۰۰ |
| شمال (جادهٔ سیدی) | ۵۰۰ |
| جنوب (بلوار شهید سلیمانی) | ۵۰۰ |

## ۴. نتیجهٔ اجرا (تک-seed، سناریوی S0)

```{python}
#| label: tbl-kpi
#| tbl-cap: "خلاصهٔ KPI به تفکیک پا — سناریوی S0، تک-seed=۴۲"
import pandas as pd
df = pd.read_csv("../outputs/tables/s0_kpi_summary.csv")
df.columns = ["پا", "تعداد وسیله", "میانگین مدت سفر (ثانیه)",
              "میانگین تلف‌شدگی زمان (ثانیه)", "میانگین انتظار (ثانیه)",
              "میانگین طول مسیر (متر)"]
df.round(2)
```

![میانگین تلف‌شدگی زمان به تفکیک پا (تک-seed، تقاضای فرضی)](../outputs/figures/s0_timeloss_by_approach.png){#fig-timeloss width=85%}

هر ۲۶۰۰ وسیلهٔ تولیدشده وارد شبکه شدند و سفر خود را کامل کردند (بدون گم‌شدن یا
گیرافتادن) — نشانهٔ سلامت پایهٔ اتصال شبکه و مسیریابی. مقادیر تلف‌شدگی زمان در
این اجرا پایین‌اند چون تقاضای فرضی فعلی نسبتاً کم است؛ این عدد **هیچ معنای
مهندسی** ندارد تا زمانی که تقاضای واقعی (فاز ۳) جایگزین شود.

## ۵. محدودیت‌های این نسخه

- هندسهٔ تقاطع مستقیماً از OSM است؛ اصلاحات مبتنی بر تصویر (تعداد خط دقیق، شعاع
  گردش، موقعیت پایه‌ها) هنوز اعمال نشده‌اند (فاز ۲).
- تقاضا و نسبت‌های گردش کاملاً فرضی‌اند (فاز ۳).
- فقط یک سناریو (S0) و یک seed اجرا شده؛ **هیچ مقایسهٔ سناریویی از این گزارش
  قابل استخراج نیست** — طبق قاعدهٔ سخت ۵ پروژه.
- ناوگان همگن (فقط سواری) است؛ سهم موتورسیکلت و مدل sublane در فاز ۲ اضافه
  می‌شود.
- گزارش فارسی از قالب پیش‌فرض Quarto استفاده می‌کند (فونت Vazirmatn و قالب
  کامل RTL/xepersian در فاز ۵ نهایی می‌شود).
