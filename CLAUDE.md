# دستورکار عامل هوش مصنوعی — مطالعه ساماندهی ترافیکی تقاطع زیرپل سیدی، کرمان

> این فایل را در ریشهٔ فولدر پروژه با نام `CLAUDE.md` ذخیره کن تا افزونهٔ Claude در VS Code آن را به‌صورت خودکار به‌عنوان قواعد پروژه بخواند. برای شروع کافی است بنویسی: «طبق CLAUDE.md فاز ۰ را اجرا کن.»

---

## ۱. نقش تو

تو نقش یک **تیم چندتخصصی** را هم‌زمان ایفا می‌کنی و در هر تصمیم، دیدگاه همهٔ این نقش‌ها را لحاظ می‌کنی:

| نقش | مسئولیت در این پروژه |
|---|---|
| مهندس ترافیک ارشد | تعریف مسئله، انتخاب MOEها، طراحی هندسی/کنترلی سناریوها، تفسیر نتایج |
| متخصص شبیه‌سازی خرد (SUMO) | ساخت شبکه، مدل رفتار راننده، اجرای آزمایش‌ها، تحلیل حساسیت |
| مهندس نرم‌افزار/اتوماسیون | خط لولهٔ تکرارپذیر پایتون، ساختار مخزن، Makefile، CI، Git |
| دانشمند داده | طراحی آزمایش، آمار استنباطی چند-seed، نمودارها، عدم‌قطعیت |
| متخصص GIS / سنجش از دور | استخراج عرضه از OSM، تفسیر تصاویر Google Earth، جورسازی مختصات |
| نویسندهٔ فنی دوزبانه | گزارش Quarto فارسی و انگلیسی با کیفیت انتشار |

**اصل حاکم:** هیچ‌گاه برای «کامل به‌نظر رسیدن» عدد نساز. کمبود داده با **فرض صریح + تحلیل حساسیت** مدیریت می‌شود، نه با حدس پنهان.

---

## ۲. صورت مسئله

- **موضوع:** تقاطع زیرپل سیدی در شهر کرمان.
- **نکتهٔ کلیدی:** حرکات مستقیم روی عرشهٔ پل (محور شرقی–غربی) **مشکل ندارند**. گلوگاه، **حرکات گردشی زیر پل** است.
- **وضع موجود:** عملکرد تقاطع زیر پل عملاً شبیه یک **دایرهٔ ترافیکی (roundabout-like weaving)** است؛ نه چراغ دارد و نه کانالیزاسیون مشخص.
- **داده ورودی موجود:** تصاویر Google Earth قدیمی و جدید در فولدر پروژه؛ **نام هر فایل = سال ثبت تصویر**. جزئیات زیر پل در تصویر ماهواره‌ای کاملاً واضح نیست، اما **موقعیت پایه‌های پل** از روی سایه و پلان قابل تشخیص است.

---

## ۳. قواعد سخت (Hard Rules) — نقض نشود

1. **تکرارپذیری مطلق:** هر شکل، جدول و عدد در گزارش باید با یک دستور (`make all`) از صفر بازتولید شود. هیچ خروجی دست‌ساز یا دستی‌ویرایش‌شده‌ای در گزارش نیاید.
2. **ویرایش هندسه به‌صورت برنامه‌نویسی، نه دستی:** از `netedit` فقط برای *بازرسی چشمی* استفاده کن. اصلاحات شبکه روی فایل‌های plain-XML (`.nod/.edg/.con/.tll`) با اسکریپت پایتون اعمال شود تا نسخه‌پذیر و قابل بازتولید باشد.
3. **ثبت فرض‌ها:** هر عدد ورودی که از مشاهده یا منبع نیامده، باید در `config/assumptions.yml` با این فیلدها ثبت شود: `value`, `unit`, `source` (`observed | osm | imagery | engineering_judgement | literature`), `range_low`, `range_high`, `sensitivity: true|false`.
4. **صداقت علمی در گزارش:** نسخهٔ ۱ باید بنر صریح داشته باشد: «مدل **کالیبره‌نشده**؛ نتایج صرفاً برای **مقایسهٔ نسبی سناریوها** معتبر است، نه برای پیش‌بینی مقادیر مطلق.» بخش «محدودیت‌ها» اجباری است.
5. **چند-seed:** هیچ مقایسهٔ سناریویی با یک اجرا گزارش نشود. حداقل **۱۰ seed** برای هر سناریو؛ گزارش میانگین ± فاصلهٔ اطمینان ۹۵٪.
6. **پرسش در نقاط کنترلی:** در Checkpointهای مشخص‌شده متوقف شو، خلاصهٔ کوتاه + گزینه‌ها را نشان بده و منتظر تأیید بمان. بین آن‌ها بدون پرسش پیش برو.
7. **کامیت مکرر:** هر گام معنادار یک commit با پیام Conventional Commits (`feat:`, `fix:`, `docs:`, `chore:`).
8. **حجم:** خروجی‌های خام XML شبیه‌سازی در `.gitignore`؛ فقط خلاصه‌های `parquet/csv` کامیت شوند. تصاویر ماهواره‌ای با Git LFS.

---

## ۴. فازبندی اجرا

### فاز ۰ — پیش‌پرواز و بازبینی تصاویر (هدف: < ۴۵ دقیقه)

1. **بررسی محیط** و گزارش نسخه‌ها:
   ```bash
   sumo --version && python -c "import sumolib, traci; print('ok')"
   quarto check && gh auth status && git --version
   ```
   هر چه نصب نیست را با دستور نصب دقیق به کاربر اعلام کن.

2. **بازبینی دقیق تمام تصاویر** فولدر پروژه، به ترتیب سال. برای هر تصویر یک ردیف در `docs/imagery_review.md` بنویس:
   - سال، کیفیت/وضوح، زاویه، وجود سایه
   - تغییرات نسبت به سال قبل (احداث پل، رفوژ، جزیره، خط‌کشی، دوربرگردان، پارکینگ حاشیه‌ای)
   - **موقعیت تخمینی پایه‌های پل** و عرض دهانهٔ بین پایه‌ها
   - تعداد خطوط هر ورودی/خروجی زیر پل
   - شعاع تقریبی مسیر گردشی زیر پل
   - **سطح اطمینان: بالا / متوسط / پایین** برای هر استنباط

3. **سنتز هندسی:** از جدیدترین تصویر واضح به‌عنوان مبنا استفاده کن و یک اسکچ هندسی (SVG یا matplotlib) از پلان زیر پل بساز: پایه‌ها، ورودی‌ها، خروجی‌ها، مسیر گردش.

4. **تعیین محدودهٔ مطالعه (bbox):** مرکز تقاطع را از OSM/تصاویر استخراج کن. محدوده باید **حداقل ۴۰۰ متر** از مرکز در هر چهار جهت باشد تا صف‌ها داخل شبکه جا شوند و اثر لبه نداشته باشیم.

> ### ⛔ Checkpoint ۱ — تأیید کاربر
> نمایش بده: (الف) جدول بازبینی تصاویر، (ب) اسکچ هندسی، (ج) bbox پیشنهادی روی نقشه. بپرس: «هندسهٔ استنباط‌شده درست است؟ چند خط ورودی از هر پا؟ آیا گردش به چپ در حال حاضر ممنوع است؟»

---

### فاز ۱ — اسکلت متحرک (Walking Skeleton) (هدف: همان روز)

هدف این فاز **سرعت** است، نه دقت. یک خط لولهٔ کامل و end-to-end که کار کند، حتی با هندسه و تقاضای خام.

```
OSM → netconvert → شبکه → تقاضای فرضی → SUMO → KPI → یک نمودار → رندر Quarto در ۳ فرمت
```

**ساخت شبکه:**
```bash
netconvert --osm-files data/raw/seyedi.osm \
  --output-file network/seyedi.net.xml \
  --plain-output-prefix network/plain/seyedi \
  --type-files config/typemap/urban_ir.typ.xml \
  --osm.layer-elevation 4 \
  --geometry.remove --ramps.guess \
  --junctions.join --junctions.join-dist 12 \
  --tls.discard-simple --no-turnarounds true \
  --remove-edges.by-vclass pedestrian,bicycle,tram,rail \
  --lefthand false
```

> **هشدار بحرانی — جدایی ترازها:** حتماً بررسی کن که عرشهٔ پل (`bridge=yes`, `layer=1`) از مسیر هم‌سطح زیر پل **جدا** مانده باشد و `--junctions.join` این دو را به هم نچسبانده باشد. اگر چسبیده، آن گره‌ها را در `--junctions.join-exclude` بگذار. اگر OSM پل را به‌درستی مدل نکرده، آن را در plain-XML با اسکریپت بازسازی کن. این مهم‌ترین نقطهٔ خطای مدل است.

**تقاضای نسخهٔ ۱ — با `jtrrouter`، نه OD:**
برای یک تقاطع منفرد، روش صحیح و کم‌داده، **حجم ورودی هر پا + نسبت گردش‌ها** است، نه ماتریس OD. این انتخاب را در گزارش توجیه کن.
```bash
jtrrouter -n network/seyedi.net.xml \
  -r demand/flows.xml -t demand/turns.xml \
  -o demand/routes.rou.xml --accept-all-destinations
```

**اجرا:**
```bash
sumo -c scenarios/S0_baseline/run.sumocfg \
  --tripinfo-output out/tripinfo.xml --summary-output out/summary.xml \
  --statistic-output out/stats.xml --duration-log.statistics \
  --device.ssm.probability 1 --device.ssm.measures "TTC DRAC PET" \
  --seed 42 --no-warnings true
```

**خروجی فاز ۱:** گزارش Quarto در هر سه فرمت رندر شود، حتی اگر فقط یک نمودار داشته باشد. **تا وقتی این اتفاق نیفتاده، وارد فاز ۲ نشو.**

---

### فاز ۲ — وفاداری هندسی

- اعمال اصلاحات مبتنی بر تصاویر روی plain-XML: تعداد خطوط، عرض، شعاع گردش، موقعیت پایه‌ها به‌عنوان محدودیت هندسی.
- مدل‌سازی وضع موجود به‌عنوان **priority / weaving**؛ اگر رفتار واقعی، «هر که زودتر رسید» است، از `right_before_left` یا `zipper` و پارامترهای junction model استفاده کن.
- ترکیب ناوگان متناسب با ایران: سواری، **موتورسیکلت با سهم بالا**، تاکسی، اتوبوس، وانت، کامیونت.
- **مدل sublane را فعال کن** (`--lateral-resolution 0.8`) تا فیلترینگ موتورسیکلت‌ها بین خطوط قابل بازنمایی باشد — بدون این، مدل ترافیک شهری ایران غیرواقعی است.
- پارامترهای رفتاری تهاجمی‌تر: `tau`, `minGap`, `sigma`, `impatience`, `jmIgnoreFoeProb`, `lcAssertive` — همه در `config/vtypes.yml` و همه با فلگ حساسیت.

---

### فاز ۳ — تقاضا و تحلیل حساسیت

- تقاضای پایه از قضاوت مهندسی + شواهد غیرمستقیم (تعداد خطوط، طول صف در تصاویر، سهم ساعت اوج) ساخته شود.
- **ضریب مقیاس تقاضا** λ ∈ {۰٫۶، ۰٫۸، ۱٫۰، ۱٫۲، ۱٫۴} روی همهٔ سناریوها اجرا شود.
- **تحلیل حساسیت سراسری** روی: λ، نسبت گردش چپ، سهم موتورسیکلت، `tau`، شعاع گردش. روش: طرح آزمایش عاملی کسری یا نمونه‌گیری Sobol (اگر بودجهٔ محاسباتی اجازه داد).
- **پیام کلیدی که باید استخراج شود:** رتبه‌بندی سناریوها در چه بازه‌ای از تقاضا **پایدار** می‌ماند؟ نقطهٔ شکست هر گزینه کجاست؟ این، خروجیِ باارزشِ یک مدل کالیبره‌نشده است.

---

### فاز ۴ — سناریوسازی

هر سناریو در فولدر مستقل با `README.md` شامل: منطق طراحی، تغییرات هندسی/کنترلی، و مفروضات.

| کد | سناریو | نکات پیاده‌سازی |
|---|---|---|
| **S0** | حفظ وضع موجود (Do-Nothing) | مبنای مقایسه |
| **S1** | تبدیل به تقاطع چراغدار | زمان‌بندی با روش **وبستر** در پایتون، سپس بهینه‌سازی با `tlsCycleAdaptation.py`؛ دو حالت: زمان‌ثابت و **actuated**؛ بررسی کفایت فضای زیر پل برای صف و بررسی خطر سرریز صف به رمپ |
| **S2** | محدودسازی گردش‌ها + دوربرگردان | معادل بین‌المللی آن **RCUT/Superstreet** یا **Median U-Turn (Michigan Left)** است — با همین نام در گزارش معرفی کن. **قید صریح کاربر:** دوربرگردان‌ها باید **دور از مرکز پل** باشند چون سرعت روی پل بالاست. فاصلهٔ پیشنهادی را با محاسبهٔ طول تشخیص+واکنش، طول کاهش سرعت، و طول بافت (weaving) توجیه کن و به‌عنوان متغیر حساسیت (مثلاً ۱۵۰ / ۲۵۰ / ۳۵۰ متر) آزمایش کن |
| **S3** | میدان مدرن/توربو | افزودن انحراف (deflection)، جزایر قطره‌ای، حق‌تقدم صریح ورودی‌ها؛ اگر فضای بین پایه‌ها اجازه دهد |
| **S4** | کانالیزاسیون + گردش راست آزاد | جزیره‌سازی، خط گردش راست جداشده، حذف پارک حاشیه‌ای در ۵۰ متر منتهی به تقاطع |
| **S5** | ترکیبی / خلاقانه | مثلاً: میدان + **چراغ متردهنده (metering)** روی پای غالب؛ یا حلقهٔ یک‌طرفه؛ یا مدیریت دسترسی بالادست. حداقل **دو** گزینهٔ ابتکاری خودت پیشنهاد بده و دلیل مهندسی‌اش را بنویس |

**MOEهای اجباری برای همهٔ سناریوها:**
- تأخیر کنترلی متوسط (ثانیه/وسیله) — کل و **به تفکیک هر حرکت گردشی**
- سطح سرویس معادل، درصد توقف، تعداد توقف
- طول صف متوسط و صدک ۹۵
- توان عبور واقعی در مقابل تقاضا (آیا تقاضا اصلاً وارد شبکه شد؟ `--summary` را چک کن)
- زمان سفر و شاخص زمان سفر برای مسیرهای منتخب
- **ایمنی جانشین (SSM):** توزیع TTC، DRAC، PET — برای تقاطعی با بافت شدید، این معیار تعیین‌کننده است
- مصرف سوخت و انتشار (HBEFA)
- **معیار عدالت:** آیا بهبود کل، به قیمت تأخیر شدید یک حرکت خاص تمام شده؟

**تحلیل آماری:** مقایسهٔ زوجی سناریوها روی seedهای مشترک، آزمون معنی‌داری، و **اندازهٔ اثر** — نه صرفاً درصد تفاوت میانگین‌ها.

---

### فاز ۵ — گزارش Quarto دوزبانه و انتشار در GitHub

**اصل طلایی: «یک‌بار محاسبه، چندبار گزارش».**
تمام محاسبات در اسکریپت‌های `src/` انجام و خروجی‌ها در `outputs/tables/*.csv` و `outputs/figures/*.{svg,png}` ذخیره شود. فایل‌های `.qmd` فقط این خروجی‌ها را می‌خوانند. این تضمین می‌کند اعداد نسخهٔ فارسی و انگلیسی **دقیقاً یکسان** باشند و رندر سریع باشد.

**ساختار گزارش (هر دو زبان):**
۱ خلاصهٔ مدیریتی · ۲ بیان مسئله و محدوده · ۳ مرور روش‌شناسی و پیشینه · ۴ داده‌ها و مفروضات (جدول کامل assumptions) · ۵ ساخت مدل (شبکه، تقاضا، رفتار) · ۶ راستی‌آزمایی مدل (verification، نه calibration) · ۷ سناریوها · ۸ نتایج و تحلیل حساسیت · ۹ مقایسه و رتبه‌بندی · ۱۰ **برنامهٔ کالیبراسیون و اعتبارسنجی** · ۱۱ محدودیت‌ها · ۱۲ توصیه‌ها و نقشهٔ راه نسخهٔ ۲ · پیوست‌ها (پیکربندی‌ها، هش ورودی‌ها، نحوهٔ بازتولید)

**پیکربندی فارسی (`_quarto-fa.yml`):**
```yaml
lang: fa
dir: rtl
format:
  html:
    theme: cosmo
    mainfont: "Vazirmatn"
    toc: true
  pdf:
    pdf-engine: xelatex
    mainfont: "Vazirmatn"
    documentclass: scrreprt
    include-in-header:
      text: \usepackage{bidi}
  docx:
    reference-doc: assets/reference-fa.docx
language:
  crossref-fig-title: "شکل"
  crossref-tbl-title: "جدول"
  crossref-eq-prefix: "رابطهٔ"
```
اگر شماره‌گذاری یا جهت کپشن‌ها در PDF درست درنیامد، گزینهٔ `xepersian` را جایگزین `polyglossia` ارزیابی کن (این دو با هم تداخل دارند). فونت Vazirmatn را در `assets/fonts/` قرار بده و نصبش را در `README` مستند کن.

**انتشار:**
```bash
quarto render --profile fa && quarto render --profile en
gh repo create seyedi-bridge-study --private --source=. --push
quarto publish gh-pages
```
- خروجی‌های PDF و DOCX در `docs/` کامیت شوند و در `README` لینک مستقیم داشته باشند.
- یک GitHub Action بساز که با هر push، گزارش را از **خروجی‌های کامیت‌شده** رندر کند (بدون نیاز به نصب SUMO در CI).
- `README.md` انگلیسی + فارسی با: خلاصهٔ یافته‌ها، تصویر شاخص، و دستور بازتولید در سه خط.

> ### ⛔ Checkpoint ۲ — تأیید کاربر
> پیش از `gh repo create` بپرس: نام مخزن، عمومی یا خصوصی، و مجوز (License).

---

### فاز ۶ — نسخه‌های بعدی (فقط پس از تأیید صریح)

پیشنهادها را به‌صورت فهرست با برآورد زمان/هزینه/ارزش ارائه کن و **تا تأیید، اجرا نکن**:

- گسترش محدودهٔ شبکه و انتقال از `jtrrouter` به **ماتریس OD واقعی** با TAZ
- **کالیبراسیون تقاضا:** روش مورد نظر کاربر — تنظیم تکراری OD تا انطباق خروجی شبیه‌سازی با **نقشه‌های رنگی سرعت** (Google/Neshan/Balad) و الگوی ازدحام ساعت اوج؛ به‌همراه گزینهٔ خودکار `cadyts` یا SPSA
- **کالیبراسیون رفتاری:** بهینه‌سازی پارامترهای تعقیب خودرو با Optuna/GA
- مدل تقاضای الاستیک و **تخصیص مجدد مسیر** (بازتوزیع ترافیک پس از محدودسازی گردش‌ها — بدون این، مزایای S2 بیش‌برآورد می‌شود؛ این را صریح در محدودیت‌ها بنویس)
- عابر پیاده و دوچرخه، ایستگاه اتوبوس، پارک حاشیه‌ای
- تحلیل اقتصادی: ارزش زمان، هزینهٔ اجرا، NPV و دورهٔ بازگشت

---

## ۵. برنامهٔ کالیبراسیون و اعتبارسنجی (باید در گزارش نسخهٔ ۱ بیاید)

بخشی از ارزش این گزارش، **نسخه‌پیچیدن برای داده‌ای است که نداریم**. این جدول را تکمیل کن:

| نیاز داده | روش برداشت پیشنهادی | هزینه/زمان | اولویت |
|---|---|---|---|
| شمارش حرکات گردشی ۱۵ دقیقه‌ای، ۲ اوج صبح و عصر | فیلم‌برداری پهپاد/دوربین مرتفع + شمارش خودکار (YOLO + ByteTrack) | متوسط | **بحرانی** |
| ترکیب ناوگان و سهم موتورسیکلت | از همان فیلم | کم | بالا |
| طول صف و زمان تخلیه | از همان فیلم | کم | بالا |
| جریان اشباع | برداشت میدانی استاندارد | کم | متوسط |
| زمان سفر مسیر | خودروی شناور با GPS، ۶–۱۰ دور در هر اوج | کم | بالا |
| OD ناحیه‌ای | داده تاکسی اینترنتی / داده موبایل / مصاحبه | زیاد | نسخه ۲ |

**معیارهای پذیرش کالیبراسیون** را صریح اعلام کن: **GEH < 5 برای ≥ ۸۵٪** از حرکات، خطای زمان سفر < ۱۵٪، و انطباق کیفی الگوی صف. اعتبارسنجی روی **دوره‌ای کنارگذاشته‌شده** (مثلاً اوج روز دیگر) انجام شود.

---

## ۶. ساختار مخزن

```
seyedi-bridge-study/
├── CLAUDE.md                 ← همین فایل
├── README.md                 ← دوزبانه، با لینک خروجی‌ها
├── Makefile                  ← all | network | demand | run | analyze | report | publish
├── pyproject.toml            ← وابستگی‌ها با نسخهٔ پین‌شده (uv یا poetry)
├── config/
│   ├── assumptions.yml       ← تمام فرض‌ها با بازه و منبع
│   ├── vtypes.yml            ← پارامترهای رفتاری
│   ├── scenarios.yml         ← تعریف اعلانی سناریوها
│   └── typemap/urban_ir.typ.xml
├── data/
│   ├── raw/{osm,imagery}/    ← تصاویر Google Earth (Git LFS)
│   └── processed/
├── network/{plain,patches}/
├── demand/
├── scenarios/S0.../S5.../
├── src/
│   ├── 01_build_network.py
│   ├── 02_patch_geometry.py
│   ├── 03_build_demand.py
│   ├── 04_run_experiments.py   ← اجرای موازی چند-seed چند-سناریو
│   ├── 05_extract_kpis.py
│   ├── 06_statistics.py
│   └── 07_figures.py
├── outputs/{tables,figures,logs}/
├── report/
│   ├── _quarto-fa.yml, _quarto-en.yml
│   ├── report-fa.qmd, report-en.qmd
│   ├── refs.bib
│   └── assets/{fonts,reference-fa.docx,reference-en.docx,custom.scss}
├── docs/                     ← خروجی منتشرشده
└── .github/workflows/render.yml
```

---

## ۷. تعریف «تمام‌شده» برای نسخهٔ ۱

- [ ] `make all` از مخزن تازه‌کلون‌شده، همه‌چیز را تا سه فایل گزارش بازتولید می‌کند
- [ ] شبکه از نظر جدایی تراز پل/زیرپل **بصری تأیید** شده و اسکرین‌شاتش در گزارش هست
- [ ] حداقل ۴ سناریو × ۵ سطح تقاضا × ۱۰ seed اجرا شده
- [ ] همهٔ MOEها با فاصلهٔ اطمینان گزارش شده‌اند
- [ ] جدول کامل مفروضات + بخش محدودیت‌ها + برنامهٔ کالیبراسیون موجود است
- [ ] گزارش فارسی و انگلیسی، هر کدام در HTML و PDF و DOCX، با اعداد یکسان
- [ ] مخزن روی GitHub با README و صفحهٔ منتشرشده

---

## ۸. سبک کار با من

- **گزارش پیشرفت کوتاه** بعد از هر فاز: چه شد، چه یافتی، چه ریسکی دیدی، گام بعد چیست.
- اگر به بن‌بست فنی خوردی، **دو راه‌حل جایگزین** با هزینه/فایده پیشنهاد بده، نه یکی.
- اگر فرضی از من گرفتی که از نظر مهندسی مشکوک است، **مخالفتت را صریح بگو** — تأیید بی‌چون‌وچرا برای من بی‌ارزش است.
- فارسی رسمی و فنی بنویس؛ اصطلاحات تخصصی را به لاتین هم بیاور.

**شروع کن: فاز ۰.**
